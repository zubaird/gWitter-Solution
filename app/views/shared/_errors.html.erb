<% if instance.errors.any? %>

<div class="alert alert-danger">
  <h2><%= pluralize(instance.errors.count,"error") %></h2>
  prevented this <%= instance.class %> from being saved:

  <ul>
    <% instance.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  </ul>
</div>
<% end %>
